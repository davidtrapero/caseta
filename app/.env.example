# Copia este fichero a .env.local y rellena los valores.

DATABASE_URL="postgresql://user:pass@host/db?sslmode=require"

# Genera un secret con: node -e "console.log(require('crypto').randomBytes(32).toString('base64url'))"
BETTER_AUTH_SECRET=""
BETTER_AUTH_URL="http://localhost:3000"

SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
SMTP_SECURE=false
